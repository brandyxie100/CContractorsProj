---
description: TypeScript frontend standards - Vite React, strict types, Tailwind
globs: frontend/**/*.{ts,tsx}
alwaysApply: false
---

# TypeScript Frontend Standards

## Code Style

- Formatter: Prettier (printWidth: 100, singleQuote: true)
- Linter: ESLint with @typescript-eslint recommended
- Type checking: strict mode — `any` is BANNED, use `unknown` with type guards
- Imports: absolute paths with `@/` alias (e.g., `@/components/ui/Button`)
- One component per file, filename matches component name
- Stack: Vite + React + React Router (SPA), not Next.js

## Component Patterns

- **Container/Presenter**: `AssetsContainer` (data + logic) + `AssetsView` (pure UI)
- **Custom Hooks**: `useAuth()`, `useAssets()`, `useDispatchSearch()` for reusable logic
- **Providers**: `AuthProvider`, `QueryClientProvider` via React Context / TanStack Query

## Naming

- `camelCase`: variables, functions, hooks
- `PascalCase`: components, types, interfaces
- `UPPER_SNAKE_CASE`: constants

## Testing (Vitest + React Testing Library)

```typescript
describe('StatusBadge', () => {
  it('should render available status with green styling', () => { ... });
  it('should show label text for maintenance', () => { ... });
});
```

- Test user interactions, not implementation details
- Prefer MSW for API mocking when adding network-level tests

## Forbidden

- `any` type — use `unknown` with type guards
- `localStorage` for tokens — access token in memory, refresh token in httpOnly cookie
- Direct API calls in presentational components — use custom hooks
- Inline styles — use Tailwind CSS utility classes
